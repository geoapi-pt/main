{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Build frontend assets and Start",
      "request": "launch",
      "command": "npm start -- --buildFeAssets",
      "cwd": "${workspaceFolder}"
    }
  ]
}